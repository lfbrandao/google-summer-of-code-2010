<h1 id="siteName">Internet Data Directory || </h1>  

<div id="nav_nav">
	Directory:
	<%= link_to "Search", :controller => "hosts", :action => "search" %>  
	<% if can? :manage, :all%>
		|
		Manage:
	<% end %>	
	<% if can? :manage, User%>
		<%= link_to "Users", users_path %>
	<% end %>
	<% if can? :manage, Role%>
		<%= link_to "Roles", roles_path %>  
	<% end %>
	<% if can? :manage, Repository%>
		<%= link_to "Repositories", repositories_path %>  
	<% end %>
	<% if can? :manage, Plugins%>
		<%= link_to "Plugins", plugins_path %>  
	<% end %>
</div>